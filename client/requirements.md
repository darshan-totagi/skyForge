## Packages
(none needed)

## Notes
Tailwind Config - if font-display isn't natively supported, we've added a utility class `.font-display` in `index.css`.
All required packages (framer-motion, lucide-react, react-hook-form, zod, etc.) are already installed.
